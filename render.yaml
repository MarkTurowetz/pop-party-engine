services:
  - type: web
    name: pop-party
    runtime: node
    plan: free
    buildCommand: npm run build-info
    startCommand: node server.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 22
