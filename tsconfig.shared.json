{
  "comment": "Builds dual-use TypeScript sources to committed CommonJS+global JavaScript beside each source (npm run build:shared), including package-owned shared contracts. Separate from the client tsconfig because these need Node's `module` global and emit, where the client config is DOM-only + noEmit. check:shared-fresh guards committed JavaScript against drift.",
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "lib": ["ES2020", "DOM"],
    "types": ["node"],
    "rootDir": ".",
    "strict": true,
    "noImplicitAny": false,
    "skipLibCheck": true,
    "removeComments": false,
    "newLine": "lf",
    "declaration": false,
    "noEmitOnError": true
  },
  "include": ["shared/**/*.ts", "packages/engine/src/shared/**/*.ts"],
  "exclude": ["shared/**/*.test.ts", "packages/engine/src/shared/**/*.test.ts"]
}
