"use strict";

// Temporary source-only metadata for the reference Art Manager and the
// one-time legacy bundle exporter. Runtime configuration must never import it.
module.exports = require("./game-data");
