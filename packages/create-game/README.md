# @pop-party/create-game

This package powers `npm create @pop-party/game MyGame`. It creates an independent game directory, pins an exact `@pop-party/engine` version, and byte-copies the canonical starter content without symlinks or references to the engine repository.
